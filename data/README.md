# Local data

Place sample images or videos for local worker experiments here. Raw media is local input and is intentionally ignored by Git. Privacy mode keeps those frames inside the worker process/container; the aggregator receives detection metadata only.
